o(n*d)

 void leftRotatebyOne(int arr[], int n) 
    { 
        int i, temp; 
	for(int i=0;i<d;i++)
	{
               temp = arr[0]; 
              for (i = 0; i < n - 1; i++) 
                    arr[i] = arr[i + 1]; 
               arr[i] = temp; 
    } 
}
